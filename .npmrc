# .npmrc is auth/registry only in pnpm 11+.
# nodeLinker and publicHoistPattern moved to pnpm-workspace.yaml.
# vite-plus preview build registry bridge (auto-added by vp)
registry=https://registry-bridge.viteplus.dev/
